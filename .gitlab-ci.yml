before_script:
   - apt install build-essential cmake -qq -y
   - cmake .
   - make


build:ubuntu:
  image: ubuntu:latest
  script:
    - ./Release/swiftfetch

build:debian:
  image: debian:latest
  script:
    - ./Release/swiftfetch

build:alpine:
  image: alpine:latest
  script:
    - ./Release/swiftfetch
