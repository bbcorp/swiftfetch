before_script:
   - apt update
   - apt install build-essential cmake -qq -y
   - cmake .
   - make


build:ubuntu:
  image: ubuntu:latest
  script:
    - ./Release/swiftfetch

build:debian:
  image: debian:latest
  script:
    - ls -l .
    - ./Release/swiftfetch

build:alpine:
  image: alpine:latest
  script:
    - ls -l .
    - ./Release/swiftfetch
